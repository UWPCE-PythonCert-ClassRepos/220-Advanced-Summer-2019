"""Market Prices Data"""


def get_latest_price(item_code):
    """Raise an exception to force the user to Mock its output"""
    if not item_code:
        print("This if and print statement only here so item_code gets used")
    return 24
